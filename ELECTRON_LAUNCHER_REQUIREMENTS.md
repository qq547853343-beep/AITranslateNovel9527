# AITranslateNovel9527 ver0.2 Electron 启动器需求与实现状态

## 1. 文档信息

- 项目：AITranslateNovel9527
- 当前版本：ver0.2
- 实现状态：已完成并通过自动化测试、解包版运行和 NSIS 打包验证
- 目标平台：Windows
- 桌面技术：Electron
- 现有服务：Node.js + Express + Vue 3 + SQLite
- 启动器管理端口：固定 7000
- 翻译服务端口：默认 6501，可修改
- 自动更新：本期只预留接口，将来实现

本文档只描述 Electron 启动器需求。大型文档翻译方案不属于本次范围。

## 2. 总体目标

增加一个 Windows 桌面启动器，让用户通过窗口完成翻译服务的启动、关闭、重启、端口配置、日志查看和开机自启动，不需要手动打开 CMD。

Electron 启动器负责进程生命周期和本地管理能力；现有 `server.js` 继续负责翻译、规则集、数据库、图片、导入和导出等业务功能。

```text
Electron 主进程
├─ 全局单实例保护
├─ 启动、关闭、重启 server.js
├─ 端口检测和切换
├─ 日志采集
├─ 开机自启动和系统托盘
└─ 自动更新预留接口

Electron 渲染进程
└─ Vue 启动器管理界面

Node.js server.js 子进程
└─ 现有翻译服务
```

## 3. 必须保留的现有功能

不得破坏当前 ver0.1 的翻译、规则集、长文本任务、图片、导入、导出、SQLite、API Key 加密和本机监听功能。

特别要求：

- 不改变现有翻译接口的兼容行为。
- 不把 API Key 放入启动器命令行、日志或前端状态。
- `data/app.db`、`data/.master-key`、图片和日志继续保存在数据目录。
- `LARGE_DOCUMENT_TRANSLATION_PLAN.md` 不得修改、删除、提交或纳入本次实现。

## 4. 桌面端形态

- 使用 Electron 生成 Windows `.exe`。
- 正式用户界面不是 CMD。
- CMD 只保留开发调试入口，例如 `--console` 模式。
- Electron 主进程负责文件、子进程、端口、日志和系统能力。
- 渲染进程通过安全 preload/API 与主进程通信，不直接开放 Node.js 能力。
- 尽可能复用当前 Vue 页面和样式。
- 支持系统托盘。

## 5. 进程管理

`server.js` 作为由启动器管理的子进程运行。启动器必须记录子进程 PID，只允许结束自己启动并记录的服务进程，不得根据模糊进程名或未知端口强制结束其他程序。

服务状态至少包括：

```text
stopped
starting
running
stopping
restarting
switching-port
failed
unknown
```

只有在以下三项都成功时，才显示“服务正常”：

1. 子进程存在。
2. 服务端口已监听。
3. HTTP 健康检查成功。

## 6. 启动、关闭和重启

### 启动

1. 读取保存的服务端口。
2. 验证端口范围以及与启动器端口的冲突。
3. 检查服务端口是否被未知进程占用。
4. 启动 `server.js` 子进程。
5. 等待端口监听成功。
6. 执行健康检查。
7. 显示 PID、端口、启动时间和健康状态。

### 关闭

1. 状态变为 `stopping`。
2. 发送优雅关闭信号或受保护的本地关闭指令。
3. 等待请求完成、数据库关闭和子进程退出。
4. 验证服务端口已经释放。
5. 超时后，只能结束已确认属于本启动器的子进程。

### 重启

```text
停止服务
→ 等待旧进程退出
→ 检查旧端口释放
→ 使用保存的端口重新启动
→ 健康检查
→ 返回最终状态
```

## 7. 端口配置、监听和保存

### 端口定义

- 启动器管理端口固定为 7000，ver0.2 不提供运行中切换。
- 翻译服务端口默认 6501，允许用户修改。
- 两个端口不能相同。
- 两个端口都只绑定 `127.0.0.1`。
- 不自动随机切换到其他端口。

### 保存

复用现有 SQLite `SettingsRepository` 保存：

```json
{
  "launcherPort": 7000,
  "servicePort": 6501,
  "autoStartLauncher": true,
  "autoStartService": true,
  "startHidden": true,
  "autoRestartService": false,
  "updatedAt": "..."
}
```

### 端口状态

```text
free
occupied-by-unknown
occupied-by-service
starting
releasing
error
```

端口预检测只是提示，最终以实际监听结果为准。端口检测必须由启动器提供，使翻译服务停止时管理器仍然可用。

### 端口切换

端口切换必须使用统一的互斥操作锁：

```text
获取操作锁
→ 验证新端口
→ 检查新端口占用
→ 停止当前服务
→ 等待旧端口释放
→ 再次检查新端口
→ 启动新端口服务
→ 健康检查
→ 成功后保存新配置
→ 释放操作锁
```

失败时：

- 旧端口未释放：不启动新服务，不覆盖有效配置。
- 新端口启动失败：回滚配置，并尝试恢复原端口服务。
- 未知进程占用端口：不强制关闭，要求用户选择其他端口。
- 并发操作返回 `409 Conflict` 或等价的忙碌状态。

## 8. 启动器接口

推荐提供以下本地管理接口或等价 IPC：

```text
GET  /api/launcher/status
GET  /api/launcher/settings
PUT  /api/launcher/settings
POST /api/launcher/start
POST /api/launcher/stop
POST /api/launcher/restart
GET  /api/launcher/logs
GET  /api/launcher/events
GET  /api/launcher/ports/check
GET  /api/launcher/update/status
POST /api/launcher/update/check
```

自动更新接口本期只返回预留状态，不下载、安装、替换或回滚程序。

现有翻译、规则、任务、图片和导出接口必须继续有效。端口检测不能只依赖翻译服务，因为服务停止时启动器仍然要可用。

## 9. 日志

启动器接管服务子进程 stdout 和 stderr，并保存到：

```text
data/logs/launcher.log
data/logs/service-YYYY-MM-DD.log
```

日志至少记录：

- 启动器启动和退出。
- 服务启动、关闭和重启。
- 服务 PID、端口、启动时间和退出码。
- 端口检测、旧端口释放和健康检查结果。
- 启动失败、重启失败和端口切换回滚原因。

默认策略：

- 保留 7 天。
- 单文件最大 10 MB。
- 最多保留 5 个历史文件。
- 页面默认显示最近 200 行。
- 支持实时刷新、手动刷新和错误/警告筛选。
- “清空显示”不等于删除日志文件。
- 脱敏 API Key、Authorization 和其他敏感字段。

## 10. 开机自启动和系统托盘

开机自启动拆分为两个选项：

1. 开机启动 Electron 启动器。
2. 开机后自动启动翻译服务。

默认行为：

- Windows 用户登录后启动启动器。
- 启动器默认隐藏到系统托盘。
- 开启“自动启动翻译服务”后才启动 `server.js`。
- 不把 `server.js` 单独注册为开机启动项。

窗口行为：

- 点击关闭按钮：隐藏到托盘，不退出启动器。
- 托盘“打开”：显示管理窗口。
- 托盘“退出”：停止翻译服务后退出启动器。
- 服务正在翻译时退出，应提示用户确认。

## 11. 全局单实例保护

启动器必须实现全局单实例保护：

- 同一 Windows 用户只能运行一个启动器实例。
- 第二次启动应激活已有窗口，不启动第二个服务。
- 单实例保护不能只按项目目录加锁。
- 异常退出后可识别并恢复失效锁。
- 只有一个启动器实例可以执行服务操作。
- 启动、关闭、重启、保存端口和端口切换共用操作锁。

优先使用 Electron 的单实例机制；如需辅助保护，再增加带 PID 校验的用户级锁文件。

## 12. 异常恢复和自动重启

启动器检测服务异常退出时显示退出时间、退出码、最后错误日志和端口状态。

自动重启为可选设置，默认关闭。开启后：

- 连续最多自动重启 3 次。
- 使用递增等待时间。
- 端口占用、配置错误和 API Key 缺失等确定性错误不得无限重启。
- 达到次数后进入 `failed` 状态并通知用户。

## 13. 安全要求

- 启动器和翻译服务只监听 `127.0.0.1`。
- 管理接口只接受本机请求。
- 渲染进程不能直接调用 Node.js 文件系统或进程 API。
- 不允许通过接口执行任意系统命令。
- 不允许传入任意 Node.js 参数。
- 不允许根据模糊进程名结束其他程序。
- API Key 不得出现在命令行、日志、前端状态或错误响应中。

## 14. 打包和运行

- 使用 `electron-builder` 生成 Windows 安装包。
- 至少生成安装版，必要时同时生成便携版。
- 用户不需要额外安装 Node.js。
- 生产包必须携带启动器和运行翻译服务所需的文件或运行时。
- 数据库、主密钥、图片和日志不打进安装包。
- 首版可暂不实现代码签名，但应预留正式发布和签名流程。
- 自动更新只做接口和状态模型，本期不实现实际更新。

## 15. 测试要求

自动化测试至少覆盖：

- 设置保存和读取。
- 启动、关闭、重启状态转换。
- 端口占用检测。
- 旧端口释放等待和超时。
- 端口切换成功、失败和回滚。
- 并发操作互斥。
- 服务异常退出和自动重启次数。
- 日志采集、轮转和敏感信息脱敏。
- 全局单实例和失效锁恢复。
- 开机自启动设置读写。
- 现有 `pnpm test` 全部通过。

Windows 手动验证至少包括：

- 双击启动器可以打开窗口且不弹 CMD 黑窗口。
- 服务停止时启动器仍可用。
- 启动、关闭、重启按钮状态正确。
- 端口被其他程序占用时不会误杀其他程序。
- 端口切换会等待旧端口释放。
- 快速连续点击不会生成多个服务进程。
- 重复启动启动器只保留一个实例。
- 开机启动、隐藏、托盘打开和退出行为正确。
- 不同窗口尺寸下界面可用。

## 16. 本期范围

本期实施：

- Electron 启动器。
- 服务进程管理。
- 启动、关闭、重启。
- 端口保存、检测和安全切换。
- 日志查看和保存。
- 开机自启动和系统托盘。
- 全局单实例保护。
- 异常状态和可选自动重启。
- 自动更新接口预留。

本期不实施：

- 自动更新的实际下载、安装和回滚。
- 大型文档翻译候选方案。
- 云端部署、PostgreSQL、云端 KMS、多用户和远程管理。
- 强制关闭未知占用端口的进程。
- 将翻译服务改造成 Windows Service。

## 17. ver0.2 实施结果

ver0.2 已实现本文件第 1 至 16 节定义的范围：

- Electron 主进程、隔离 preload、Vue 管理页面和系统托盘。
- `server.js` 受控子进程、PID/实例 ID/私有令牌记录和优雅关闭。
- 固定管理端口 7000、默认服务端口 6501、端口检测与切换回滚。
- 统一异步操作锁、Electron 单实例和用户级全局 PID 锁。
- SQLite 启动器设置、Windows 登录项、启动时隐藏和可选自动启动服务。
- Launcher/Service 日志轮转、最近 200 行读取及敏感信息脱敏。
- 异常退出状态和最多三次的可选自动重启。
- electron-builder Windows x64 解包版与 NSIS 安装包。
- 自动更新 IPC 和状态占位，不包含更新执行能力。

验证基线：

- `pnpm test`：28 项全部通过。
- 解包版可以启动带唯一实例 ID 的真实翻译服务子进程。
- 不同用户数据目录重复启动时，全局锁仍会拒绝第二个启动终端。
- 管理页面在桌面、最小桌面和移动尺寸下没有横向溢出。
- 安装包不包含运行数据库、主密钥、用户图片、日志或大型文档候选方案。

当前发布限制：安装包尚未配置代码签名和正式产品图标；自动更新仍是占位接口。
